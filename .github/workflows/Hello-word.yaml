name: Hello word

on:
  push:
      branches: ["**"]

jobs:
  my_first_job: 
      runs-on: "ubuntu-latest"
      steps:
        - name: Hello step
          run: echo "Helo world :)"
steps:
- uses: actions/checkout@v4